# -*- mode: snippet -*-
# name: YGPassSecret
# key: YGPassSecret
# --
apiVersion: yougov.com/v1alpha2
kind: YGPassSecret
metadata:
  name: secret-name
  namespace: {{ .Release.Namespace }}
spec:
  data:
  - name: SECRET_KEY
    resource: {{ .Values.pmpAccount.resource }}
    username: {{ .Values.pmpAccount.user }}