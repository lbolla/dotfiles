# set username
set postmaster "lbolla"
# set polling time (5 minutes)
set daemon 600
set logfile .fetchmailrc.log

poll pop.gmail.com with proto POP3
   user 'mail-address' there with password 'your-password' is lbolla here options ssl
